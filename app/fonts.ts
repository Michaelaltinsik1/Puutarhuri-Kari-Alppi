import { Allura, Libre_Baskerville } from 'next/font/google';

export const allura = Allura({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
});

export const libre_baskerville = Libre_Baskerville({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
});
